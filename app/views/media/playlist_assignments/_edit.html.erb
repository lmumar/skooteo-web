<div
  id="playlistassignment_form_modal"
  class="s-modal"
  role="modal"
  data-controller="playlist-video-select closer"
  data-playlist-video-select-endpoint-value="<%=media_playlist_assignments_path(playlist_id: @playlist.id)%>"
>
  <%= render 'form' %>
</div>
<%= turbo_stream_from Current.user, 'media:playlist_assignment' %>
